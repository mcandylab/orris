{
  "name": "orris",
  "version": "1.0.0",
  "description": "Real-time multiplayer browser IO game",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:shared": "npm run build -w shared",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
